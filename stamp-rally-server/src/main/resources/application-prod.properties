# 배포용: JAR 내부 classpath 정적 리소스만 사용 (기본값과 동일 동작)
spring.web.resources.static-locations=classpath:/static/

# 키오스크 QR이 휴대폰에서 열릴 실제 공개 URL (https 도메인). 비우면 요청 Host 기준.
# app.photo-booth.public-base-url=https://your-domain.example
